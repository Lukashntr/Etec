# Atualizando dados na tabela PROPRIETARIO

UPDATE Proprietario SET endereco = 'Avenida Paulista' WHERE id_dono = 1;

# Atualizando dados na tabela CATEGORIA

UPDATE categoria SET categoria_veiculo = ' SEDÃ ' WHERE id_categoria = 1;

# Atualizando dados na tabela MARCA

UPDATE marca SET marca = 'FIAT' WHERE id_marca = 1;

# Atualizando dados na tabela ESTADO

UPDATE estado SET nome_estado = 'Bahia' WHERE id_estado = 1;

# Atualizando dados na tabela MUNICIPIO

UPDATE municipio SET nome_municipio = 'Santa Cruz Da Vitoria' WHERE id_estado = 1;

# Atualizando dados na tabela VEICULO

UPDATE veiculo SET PlacaVeiculo = 'BONITO0704' WHERE id_veiculo = 1;

